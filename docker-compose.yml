version: '3.8'

services:
  web:
    env_file: deck_api_perm_project/.env 
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
 